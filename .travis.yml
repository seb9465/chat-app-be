language: nodejs

cache:
  directories:
    - "node_modules"

install:
- npm install -g @angular/cli
- nvm install v8.9.1

before_script:
  - npm install
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
- npm run postinstall
